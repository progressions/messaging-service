-- Optional initialization script for local Postgres.
-- The postgres:15-alpine image already creates the database and user from
-- environment variables in docker-compose.yml.
-- Add schema/migrations here if you prefer raw SQL during container init.

-- Example: ensure timezone
-- SET TIME ZONE 'UTC';

